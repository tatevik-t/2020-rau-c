#include <stdio.h>

int main(){
    // we can define the new name to a specific type to make it more convinient to code
    typedef unsigned long long unsignedll;
    
    // then we can use both old and new names
    unsignedll l;
    unsigned long long l;

    return 0;
}